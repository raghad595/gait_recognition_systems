# Gait Sentinel Frontend

## Local development

1. Start the backend on `http://localhost:5000`
2. Start this frontend with `npm run dev`
3. Open the Vite URL, usually `http://localhost:8080`

This project proxies `/api` requests to `http://localhost:5000` during local development, which avoids CORS issues for the frontend.
